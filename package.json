{
  "name": "herdr-discord-presence",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "imports": {
    "#src/*": "./src/*.ts"
  },
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node src/index.ts",
    "typecheck": "tsc --project tsconfig.json",
    "test": "node --test test/*.test.ts",
    "check": "npm run typecheck && npm test"
  },
  "dependencies": {
    "@xhayper/discord-rpc": "1.3.4"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "typescript": "^7.0.2"
  }
}
